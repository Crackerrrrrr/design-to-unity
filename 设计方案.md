# Design Handoff MCP 设计方案

## 1. 项目定位

本项目不直接操作 Unity、Cocos、Godot、Unreal Engine 或前端工程，也不直接生成最终可用的预制体、场景、Widget、组件文件。

本项目的核心定位是：

> 从蓝湖设计稿中提取足够完整、结构化、低歧义的设计信息，并生成面向多端落地的 Design Implementation Packet，让 AI 能够基于这些信息调用对应平台 MCP 完成实际落地。

也就是说，本项目是一个“设计稿上下文与交付规则 MCP”，而不是某个平台的专用转换器。

目标平台可以包括：

- Unity
- Cocos Creator
- Godot
- Unreal Engine UMG
- Web 前端
- 后续可扩展到 Flutter、iOS、Android、React Native 等

## 2. 职责边界

### 2.1 本 MCP 负责

- 解析蓝湖链接。
- 获取蓝湖项目中的设计图列表。
- 获取指定设计图的 schema / sketch json / dds 数据。
- 下载图片、切图、图标等资源。
- 将蓝湖原始图层数据标准化为平台无关的 Design IR。
- 生成资源清单 Asset Manifest。
- 提供图层树、节点详情、样式信息、文本信息、坐标信息。
- 识别候选语义，比如按钮、弹窗、列表、标题、图标、背景、装饰图。
- 根据目标平台返回落地规则 Handoff Profile。
- 给 AI 提供执行建议，让 AI 更好调用 Unity MCP / Cocos MCP / Godot MCP / UE MCP / 前端工具。

### 2.2 本 MCP 不负责

- 不直接修改 Unity prefab。
- 不直接创建 Unity scene。
- 不直接操作 Cocos 场景或节点。
- 不直接操作 Godot scene。
- 不直接创建 Unreal Widget Blueprint。
- 不直接写入业务代码。
- 不直接绑定项目已有脚本。
- 不直接手写 Unity prefab YAML、Godot tscn、Unreal uasset 等平台私有文件。

这些动作应该交给对应平台 MCP 或平台编辑器插件完成。

## 3. 总体架构

```text
Lanhu Source Adapter
  ↓
Raw Design Data
  ↓
Design Normalizer
  ↓
Design IR
  ↓
Asset Downloader / Asset Manifest
  ↓
Semantic Analyzer
  ↓
Design Implementation Packet
  ↓
Target Handoff Profile
  ↓
AI 调用目标平台 MCP 执行落地
```

### 3.1 Lanhu Source Adapter

负责与蓝湖接口交互，包括：

- 解析 URL 中的 `tid`、`pid`、`image_id`。
- 获取设计图列表。
- 获取分组信息。
- 获取设计图版本号。
- 获取 DDS schema。
- 获取原始 sketch json。
- 获取切图下载地址。

### 3.2 Design Normalizer

将蓝湖不同格式的数据统一转换成平台无关结构。

需要兼容：

- Figma 格式
- Sketch 格式
- Photoshop 格式
- 蓝湖 DDS schema 格式

统一后，每个图层都应该具备：

- 唯一 ID
- 名称
- 类型
- 图层路径
- 坐标
- 尺寸
- 层级
- 可见性
- 样式
- 文本
- 图片资源引用
- 子节点

### 3.3 Asset Downloader

负责下载图片资源，并生成平台无关资源清单。

第一版建议只下载到本 MCP 的缓存目录，也可以支持用户传入目标工程资源目录。

资源清单中必须记录：

- 远程 URL
- 本地路径
- 原始尺寸
- 逻辑尺寸
- 格式
- 倍率
- 是否透明
- 建议命名
- 推荐导入设置

### 3.4 Semantic Analyzer

负责做轻量语义识别，但不做强制转换。

例如：

- 名称包含 `btn`、`button`、`按钮`，标记为 `button_candidate`。
- 大面积底图标记为 `background_candidate`。
- 位于顶部的大文本标记为 `title_candidate`。
- 多个结构相似的节点标记为 `list_item_candidate`。
- 遮罩 + 居中面板结构标记为 `dialog_candidate`。

语义识别只提供 hint，不强制 AI 或目标平台 MCP 执行绑定。

## 4. 核心产物：Design Implementation Packet

Design Implementation Packet 是本 MCP 的核心输出。

它是一份平台无关的设计落地说明书，包含设计事实、资源、图层树、语义提示和平台交接规则。

### 4.1 顶层结构

```json
{
  "packet_id": "lanhu_project_design_version",
  "source": {
    "provider": "lanhu",
    "project_id": "xxx",
    "team_id": "xxx",
    "design_id": "xxx",
    "version_id": "xxx",
    "url": "https://lanhuapp.com/..."
  },
  "design": {
    "name": "HomePage",
    "width": 750,
    "height": 1334,
    "scale": 2,
    "unit": "px",
    "coordinate_system": "top-left"
  },
  "nodes": [],
  "assets": [],
  "semantic_map": {},
  "handoff_profiles": {},
  "warnings": []
}
```

### 4.2 Design Node

```json
{
  "id": "layer_123",
  "name": "btn_start",
  "path": "Root/Footer/btn_start",
  "type": "image",
  "semantic_type": "button_candidate",
  "visible": true,
  "z_index": 18,
  "rect": {
    "x": 48,
    "y": 720,
    "width": 280,
    "height": 88
  },
  "style": {
    "opacity": 1,
    "fill_color": "rgba(255,255,255,1)",
    "corner_radius": 12,
    "shadow": null,
    "border": null
  },
  "text": null,
  "asset_ref": "asset_btn_start",
  "children": []
}
```

### 4.3 Text Node

```json
{
  "id": "layer_title",
  "name": "txt_title",
  "type": "text",
  "rect": {
    "x": 80,
    "y": 120,
    "width": 300,
    "height": 60
  },
  "text": {
    "content": "开始游戏",
    "font_family": "PingFangSC-Semibold",
    "font_size": 32,
    "font_weight": 600,
    "color": "rgba(255,255,255,1)",
    "align": "center",
    "line_height": 38,
    "letter_spacing": 0
  },
  "style": {
    "opacity": 1
  }
}
```

### 4.4 Asset Manifest Item

```json
{
  "id": "asset_btn_start",
  "name": "btn_start",
  "type": "image",
  "remote_url": "https://...",
  "local_path": "./data/assets/HomePage/btn_start.png",
  "suggested_project_path": "Assets/DesignHandoff/HomePage/Sprites/btn_start.png",
  "format": "png",
  "size": {
    "width": 560,
    "height": 176
  },
  "logical_size": {
    "width": 280,
    "height": 88
  },
  "scale": 2,
  "has_alpha": true,
  "usage": "button_candidate",
  "import_hints": {
    "unity": {
      "texture_type": "Sprite",
      "sprite_mode": "Single",
      "alpha_is_transparency": true,
      "pixels_per_unit": 100
    },
    "cocos": {
      "type": "spriteFrame",
      "packable": true
    },
    "web": {
      "loading": "eager",
      "css_size": "280px 88px"
    }
  }
}
```

## 5. MCP 工具设计

### 5.1 设计图发现

```text
lanhu_design_list(url)
```

用途：

- 获取蓝湖项目中的设计图列表。
- 返回设计图 ID、名称、尺寸、分组、更新时间、序号。

返回重点：

- 设计图列表。
- 是否数量过多。
- 建议用户选择具体设计图。

### 5.2 准备设计交付包

```text
lanhu_design_prepare_packet(
  url,
  design_name_or_index,
  target = "unity|cocos|godot|unreal|web|generic",
  asset_output_dir = null,
  include_hidden = false,
  image_scale = "2x"
)
```

用途：

- 拉取指定设计图数据。
- 下载资源。
- 标准化节点树。
- 生成 Design Implementation Packet。
- 返回 `packet_id` 和摘要。

这是第一版最核心的工具。

### 5.3 获取完整 Packet

```text
lanhu_design_get_packet(packet_id)
```

用途：

- 返回完整 Design Implementation Packet。
- 适合节点较少的设计图。

如果节点太多，应返回提示，建议使用分页工具。

### 5.4 获取节点树

```text
lanhu_design_get_node_tree(
  packet_id,
  max_depth = 3,
  include_style = true,
  include_children = true
)
```

用途：

- 让 AI 分阶段读取设计结构。
- 避免一次返回过多 token。

### 5.5 获取节点详情

```text
lanhu_design_get_node_detail(
  packet_id,
  node_ids
)
```

用途：

- 查询指定节点的完整样式、文本、图片、语义、坐标。
- 适合 AI 对重点区域做精确落地。

### 5.6 获取资源清单

```text
lanhu_design_get_asset_manifest(packet_id)
```

用途：

- 返回所有资源路径、下载状态、尺寸、倍率、推荐导入设置。
- 供目标平台 MCP 下载、复制或导入资源。

### 5.7 获取平台交接规则

```text
lanhu_design_get_handoff_profile(
  packet_id,
  target
)
```

用途：

- 根据目标平台返回专门的坐标转换、组件映射、资源导入、文本映射、层级规则。

### 5.8 获取执行计划

```text
lanhu_design_get_implementation_plan(
  packet_id,
  target,
  mode = "static|componentized|adaptive"
)
```

用途：

- 给 AI 一份“应该如何调用目标平台 MCP”的建议计划。
- 不直接执行。

## 6. 平台 Handoff Profile

### 6.1 通用规则

所有目标平台默认遵循：

- 不使用远程 URL。
- 先导入资源，再创建节点。
- 保留设计图层级和视觉顺序。
- 保留原始设计尺寸、位置、颜色、字体信息。
- 对无法精确支持的效果生成 warning。
- 对疑似可交互组件只标记候选，不擅自绑定业务逻辑。
- 优先生成静态 View，再根据用户需求增强为组件。

### 6.2 Unity Profile

目标执行器：Unity MCP。

建议规则：

```text
- 使用 UGUI。
- 文本使用 TextMeshProUGUI。
- 图片使用 Image + Sprite。
- 根节点尺寸等于设计画布尺寸。
- 默认 Canvas 左上角坐标系。
- anchorMin = (0, 1)
- anchorMax = (0, 1)
- pivot = (0, 1)
- anchoredPosition = (x, -y)
- sizeDelta = (width, height)
- 图层按 Unity plan 的 create_nodes 顺序创建；同父节点内 z_index 越大越先创建，z_index 越小越靠上。
- button_candidate 仅作为候选，是否添加 Button 组件由用户或 Unity MCP 决定。
- list_item_candidate 可作为后续组件化依据，但第一版按静态节点创建。
```

组件映射：

```text
group -> GameObject + RectTransform
image -> GameObject + RectTransform + Image
text  -> GameObject + RectTransform + TextMeshProUGUI
shape -> GameObject + RectTransform + Image
button_candidate -> Image/Text 组合，必要时由 Unity MCP 加 Button
```

### 6.3 Cocos Creator Profile

目标执行器：Cocos MCP。

建议规则：

```text
- 使用 Node。
- 图片使用 Sprite。
- 文本使用 Label。
- 每个节点挂 UITransform。
- 根节点尺寸等于设计画布尺寸。
- 坐标需要从左上角转换到 Cocos 节点坐标。
- 默认 pivot 使用 (0, 1) 或根据 Cocos 版本转换 anchorPoint。
- SpriteFrame 使用下载后的本地资源。
```

组件映射：

```text
group -> Node + UITransform
image -> Node + UITransform + Sprite
text  -> Node + UITransform + Label
shape -> Node + UITransform + Sprite / Graphics
button_candidate -> Node + Button candidate
```

### 6.4 Godot Profile

目标执行器：Godot MCP。

建议规则：

```text
- 使用 Control 系 UI。
- 图片使用 TextureRect。
- 文本使用 Label 或 RichTextLabel。
- 按设计坐标设置 position 和 size。
- 根节点使用 Control。
- anchors 默认固定到左上。
- 对按钮候选使用 Button 或 TextureButton 由执行器按用户需求决定。
```

组件映射：

```text
group -> Control
image -> TextureRect
text  -> Label
shape -> ColorRect / TextureRect
button_candidate -> Button / TextureButton candidate
```

### 6.5 Unreal Engine UMG Profile

目标执行器：UE MCP。

建议规则：

```text
- 使用 UMG Widget。
- 根节点使用 Canvas Panel。
- 图片使用 Image。
- 文本使用 TextBlock。
- 坐标写入 CanvasPanelSlot position / size。
- anchors 默认左上。
- z_order 使用节点 z_index。
- button_candidate 可包装成 Button，但默认不绑定事件。
```

组件映射：

```text
group -> Canvas Panel / Overlay
image -> Image
text  -> TextBlock
shape -> Border / Image
button_candidate -> Button candidate
```

### 6.6 Web Frontend Profile

目标执行器：前端代码生成器或 Web MCP。

建议规则：

```text
- 第一版默认 absolute layout。
- 根容器 position: relative。
- 子节点 position: absolute。
- left = x px。
- top = y px。
- width / height 直接使用设计值。
- 图片使用本地 assets 路径。
- 文本保留 font-size、font-weight、color、line-height、text-align。
- 复杂布局后续可由 AI 基于语义转换为 flex/grid。
```

组件映射：

```text
group -> div
image -> img / div background-image
text  -> span / div
shape -> div
button_candidate -> button / div role=button candidate
```

## 7. AI 推荐工作流

### 7.1 静态页面落地

```text
1. 用户提供蓝湖链接。
2. AI 调用 lanhu_design_list。
3. AI 或用户选择设计图。
4. AI 调用 lanhu_design_prepare_packet。
5. AI 调用 lanhu_design_get_handoff_profile(target)。
6. AI 调用 lanhu_design_get_node_tree 了解结构。
7. AI 调用 lanhu_design_get_asset_manifest 获取资源。
8. AI 调用目标平台 MCP 创建页面、预制体、Widget 或组件。
9. AI 调用目标平台 MCP 截图。
10. AI 对照 Packet 中的节点和样式继续修正。
```

### 7.2 组件化落地

```text
1. 先按静态页面生成。
2. 读取 semantic_map。
3. 识别按钮、列表、弹窗、页签等候选结构。
4. 根据用户项目规范替换成已有组件。
5. 保留静态节点原始信息作为对照。
6. 生成组件化后的 diff 说明。
```

### 7.3 局部修改已有页面

```text
1. 用户指定蓝湖设计图和目标平台现有页面。
2. AI 获取 Packet。
3. AI 读取目标平台 MCP 的现有节点树。
4. AI 计算设计节点与现有节点的映射关系。
5. 只更新用户指定区域。
6. 保留业务绑定和人工修改。
```

## 8. 缓存和版本管理

### 8.1 缓存目录建议

```text
data/
  packets/
    {project_id}/
      {design_id}/
        {version_id}/
          packet.json
          node_tree.json
          asset_manifest.json
          semantic_map.json
          handoff_profiles/
            unity.json
            cocos.json
            godot.json
            unreal.json
            web.json
  assets/
    {project_id}/
      {design_id}/
        {version_id}/
          images/
          slices/
  raw/
    {project_id}/
      {design_id}/
        {version_id}/
          schema.json
          sketch.json
```

### 8.2 缓存策略

- 以蓝湖 `version_id` 作为强缓存键。
- 同一设计图同一版本重复请求时复用 packet 和资源。
- 版本变化时重新生成 packet。
- 资源下载失败时记录失败项，不让整个 packet 失败。
- 提供 force_refresh 参数强制重新拉取。

## 9. 配置项

建议环境变量：

```text
LANHU_COOKIE=...
DDS_COOKIE=...
DATA_DIR=./data
HTTP_TIMEOUT=30
DEFAULT_IMAGE_SCALE=2x
MAX_NODES_PER_RESPONSE=200
DEBUG=false
```

## 10. 第一阶段 MVP

第一阶段只做蓝湖输入和通用设计交付包，不接具体平台工程。

必须完成：

- `lanhu_design_list`
- `lanhu_design_prepare_packet`
- `lanhu_design_get_packet`
- `lanhu_design_get_node_tree`
- `lanhu_design_get_node_detail`
- `lanhu_design_get_asset_manifest`
- `lanhu_design_get_handoff_profile`
- Unity handoff profile
- Web handoff profile

第一阶段暂缓：

- Cocos / Godot / Unreal 复杂规则。
- 自动组件化。
- 复杂自适应布局。
- 双向 diff。
- 和目标平台 MCP 的自动联动。

## 11. 第二阶段能力

- 增加 Cocos / Godot / Unreal profile。
- 增加 semantic analyzer。
- 增加 list / dialog / button / tab / input 识别。
- 支持局部区域导出。
- 支持按节点下载资源。
- 支持平台执行计划生成。
- 支持 packet diff。
- 支持设计版本变更对比。

## 12. 第三阶段能力

- 支持 Figma、Sketch、本地 PSD 等更多 Source Adapter。
- 支持项目规范学习，例如命名规范、资源目录、组件库映射。
- 支持 Design Token 输出。
- 支持跨平台组件候选映射。
- 支持 AI 自动对照截图进行还原度评分。

## 13. 关键风险

### 13.1 蓝湖接口风险

蓝湖接口不是稳定公开 API，Web 端字段变化可能导致工具失效。

应对：

- 保留 raw schema。
- Normalizer 做多格式兼容。
- 错误时返回可诊断信息。

### 13.2 图层语义不稳定

设计师命名不统一，自动识别按钮、列表、弹窗可能误判。

应对：

- semantic_type 只作为候选。
- 不自动绑定业务逻辑。
- 将判断依据一并返回。

### 13.3 平台坐标差异

Unity、Cocos、Godot、UE、Web 坐标体系不同。

应对：

- Packet 中保存原始坐标。
- Handoff Profile 中单独描述转换规则。
- 不在原始 Design Node 上覆盖平台坐标。

### 13.4 复杂效果无法完整还原

渐变、阴影、混合模式、蒙版、模糊效果在不同平台支持不一致。

应对：

- 尽量提取原始参数。
- 在 warnings 中标注不确定效果。
- 必要时建议使用图片切图代替程序化还原。

## 14. 推荐命名

项目名建议：

```text
FunPlayLanhuGameMcp
```

如果未来要支持更多来源，可以升级为：

```text
FunPlayDesignHandoffMcp
```

蓝湖只是其中一个 source adapter。

## 15. 总结

本项目的核心价值不是替某个平台生成最终文件，而是让 AI 拿到足够完整的设计事实和平台落地规则。

最终协作模式应该是：

```text
Lanhu MCP 负责讲清楚设计是什么。
目标平台 MCP 负责把设计做出来。
AI 负责理解用户需求，并在两者之间做决策和调度。
```

这样才能同时支持 Unity、Cocos、Godot、Unreal Engine、Web 前端，并且保留足够灵活性来适配不同项目的工程规范。
